name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: install brew sqlite (for loadable extensions)
        run: brew install sqlite
      - run: bun install --frozen-lockfile
      - run: bun run tsc
      - run: bun test
